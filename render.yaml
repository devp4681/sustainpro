services:
  - type: web
    name: sustainpro
    env: node
    plan: starter
    buildCommand: corepack enable && pnpm install --frozen-lockfile=false && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_PATH
        value: /var/data/sustainpro.db
      - key: UPLOADS_PATH
        value: /var/data/uploads
    disk:
      name: sustainpro-data
      mountPath: /var/data
      sizeGB: 1
